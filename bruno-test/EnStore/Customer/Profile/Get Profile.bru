meta {
  name: Get Profile
  type: http
  seq: 1
}

get {
  url: {{base_url}}/customer/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
