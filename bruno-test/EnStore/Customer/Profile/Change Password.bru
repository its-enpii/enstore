meta {
  name: Change Password
  type: http
  seq: 3
}

post {
  url: {{base_url}}/customer/profile/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{customer_token}}
}

body:json {
  {
      "current_password": "password123",
      "new_password": "newpassword123",
      "new_password_confirmation": "newpassword123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
