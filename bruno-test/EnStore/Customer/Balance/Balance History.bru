meta {
  name: Balance History
  type: http
  seq: 2
}

get {
  url: {{base_url}}/customer/balance/mutations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
