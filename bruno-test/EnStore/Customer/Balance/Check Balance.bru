meta {
  name: Check Balance
  type: http
  seq: 1
}

get {
  url: {{base_url}}/customer/balance
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
