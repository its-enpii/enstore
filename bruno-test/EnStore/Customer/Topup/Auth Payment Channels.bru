meta {
  name: Auth Payment Channels
  type: http
  seq: 2
}

get {
  url: {{base_url}}/customer/transactions/payment-channels
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
