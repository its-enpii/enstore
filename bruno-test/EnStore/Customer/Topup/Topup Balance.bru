meta {
  name: Topup Balance
  type: http
  seq: 3
}

post {
  url: {{base_url}}/customer/transactions/topup
  body: json
  auth: bearer
}

auth:bearer {
  token: {{customer_token}}
}

body:json {
  {
      "amount": 150000,
      "payment_method": "QRISC"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
