meta {
  name: Get Product List
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
