meta {
  name: Get Single Product
  type: http
  seq: 2
}

get {
  url: {{base_url}}/admin/products/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
