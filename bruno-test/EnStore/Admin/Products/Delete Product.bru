meta {
  name: Delete Product
  type: http
  seq: 5
}

post {
  url: {{base_url}}/admin/products/17
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    ""
  }
}

body:multipart-form {
  _method: DELETE
}

settings {
  encodeUrl: true
  timeout: 0
}
