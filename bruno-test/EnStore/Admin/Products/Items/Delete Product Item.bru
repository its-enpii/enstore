meta {
  name: Delete Product Item
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/admin/products/items/74
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
