meta {
  name: Create New Product
  type: http
  seq: 3
}

post {
  url: {{base_url}}/admin/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
