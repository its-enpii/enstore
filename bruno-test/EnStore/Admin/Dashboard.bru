meta {
  name: Dashboard
  type: http
  seq: 4
}

get {
  url: {{base_url}}/admin/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
