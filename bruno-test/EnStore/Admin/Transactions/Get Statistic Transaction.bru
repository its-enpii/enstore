meta {
  name: Get Statistic Transaction
  type: http
  seq: 2
}

get {
  url: {{base_url}}/admin/transactions/statistics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
