meta {
  name: Update Transaction Status
  type: http
  seq: 4
}

put {
  url: {{base_url}}/admin/transactions/13/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "failed",
    "reason": "Product unavailable"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
