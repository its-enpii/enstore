meta {
  name: Adjust Balance User
  type: http
  seq: 6
}

post {
  url: {{base_url}}/admin/users/3/adjust-balance
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "type": "add",
    "amount": 50000,
    "description": "Bonus for reseller"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
