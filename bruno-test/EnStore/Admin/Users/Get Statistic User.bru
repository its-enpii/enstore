meta {
  name: Get Statistic User
  type: http
  seq: 2
}

get {
  url: {{base_url}}/admin/users/statistics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
