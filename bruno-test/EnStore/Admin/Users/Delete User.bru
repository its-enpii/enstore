meta {
  name: Delete User
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/admin/users/3
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
