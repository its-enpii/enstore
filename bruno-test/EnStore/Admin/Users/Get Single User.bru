meta {
  name: Get Single User
  type: http
  seq: 3
}

get {
  url: {{base_url}}/admin/users/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
